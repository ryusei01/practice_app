# FastAPI & Server (starlette>=0.47.2, h11>=0.15 for known CVEs)
fastapi==0.134.0
starlette>=0.47.2
h11>=0.15.0
uvicorn[standard]==0.42.0
python-multipart==0.0.22
slowapi==0.1.9
pydantic[email]==2.12.5
email-validator>=2.0.0

# Database
sqlalchemy==2.0.23
alembic==1.12.1
asyncpg==0.29.0
psycopg2-binary==2.9.11

# Supabase
supabase==2.28.3

# Authentication
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
argon2-cffi>=23.1.0
python-dotenv==1.0.0

# Stripe
stripe==7.8.0

# AI/ML (軽量のみ: numpy は recommender/predictor で使用)
numpy==1.26.2

# Translation (Google Web)
beautifulsoup4>=4.12.3

# Utilities
pydantic-settings==2.13.1
httpx==0.28.1
redis>=5.0.0

# NOTE: sentence-transformers, scikit-learn, lightgbm, pandas, joblib は
# 開発環境 (requirements.txt) のみ。本番では ENABLE_ML=false で ML機能を無効化。
