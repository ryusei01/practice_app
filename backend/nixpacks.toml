[providers]
python = "3.11"

[phases.install]
dependsOn = ["setup"]
cmds = ["pip install -r requirements.txt"]

[phases.build]
dependsOn = ["install"]

[start]
cmd = "python -m uvicorn app.main:app --host 0.0.0.0 --port $PORT"
