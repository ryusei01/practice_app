export const TensorflowPytorchQuiz_CSV = `
question_text,question_type,option_1,option_2,option_3,option_4,correct_answer,explanation,difficulty,category,subcategory1,subcategory2
What is the default execution mode in TensorFlow 2.x?,multiple_choice,Graph Execution,Eager Execution,Lazy Execution,XLA Execution,2,"TensorFlow 2.x defaults to Eager Execution, which runs operations immediately as Python statements are called. This improves debuggability and makes TF behave more like standard Python.",0.1,DL,TensorFlow,execution_mode
"In PyTorch, what is the correct order of steps in a standard training loop?",multiple_choice,loss.backward() -> optimizer.step() -> optimizer.zero_grad(),optimizer.zero_grad() -> loss.backward() -> optimizer.step(),optimizer.step() -> loss.backward() -> optimizer.zero_grad(),optimizer.zero_grad() -> optimizer.step() -> loss.backward(),2,Gradients must be zeroed before computing new ones to prevent accumulation from previous steps. Then loss.backward() computes gradients and optimizer.step() updates the weights.,0.2,DL,PyTorch,backpropagation
Which PyTorch method sets a model into evaluation mode?,multiple_choice,model.freeze(),model.predict(),model.eval(),model.inference(),3,model.eval() disables Dropout and switches BatchNorm to use running statistics instead of batch statistics. It should be paired with torch.no_grad() to also disable gradient tracking during inference.,0.2,DL,PyTorch,inference
What does tf.function() do in TensorFlow?,multiple_choice,Converts a Python function into a TensorFlow static computation graph for faster execution,Enables GPU memory allocation for a function,Freezes the weights of a model,Automatically tunes the learning rate,1,tf.function() traces the Python function and compiles it into a TensorFlow graph. This reduces Python overhead on repeated calls and enables XLA compilation for speed improvements.,0.3,DL,TensorFlow,graph_mode
Which context manager in PyTorch disables gradient computation during inference?,multiple_choice,torch.no_grad(),torch.disable_grad(),torch.grad_off(),torch.inference(),1,"torch.no_grad() prevents PyTorch from tracking operations for autograd, reducing memory usage and computation cost. It is typically used together with model.eval() during inference.",0.2,DL,PyTorch,autograd
What is the purpose of tf.GradientTape in TensorFlow?,multiple_choice,To record operations for automatic differentiation and manually compute gradients,To visualize the model architecture,To schedule the learning rate automatically,To save model checkpoints during training,1,"tf.GradientTape records operations executed inside its context. Calling tape.gradient(loss, variables) computes the gradients of loss with respect to those variables. It is essential for custom training loops.",0.4,DL,TensorFlow,GradientTape
Which Keras method saves only the weights of a model?,multiple_choice,model.save('path'),model.export('path'),model.save_weights('path'),"tf.saved_model.save(model, 'path')",3,model.save_weights() stores only the weight values. model.save() stores the full model including architecture and optimizer state. Saving weights alone is useful when the architecture is defined separately.,0.2,DL,TensorFlow,model_saving
"In PyTorch, what is the primary purpose of increasing num_workers in DataLoader?",multiple_choice,To increase the model parameter count,To parallelize data loading across CPU processes and reduce GPU idle time,To automatically increase the batch size,To enable multi-GPU training,2,"num_workers specifies how many subprocesses are used for data loading. A higher value allows data to be prepared in parallel while the GPU is busy with the previous batch, reducing the data bottleneck.",0.3,DL,PyTorch,DataLoader
What is the main advantage of PyTorch's dynamic computation graph compared to TensorFlow 1.x's static graph?,multiple_choice,It always uses less memory,It allows the graph to be modified at runtime enabling flexible debugging,It runs faster on CPU without any configuration,It automatically optimizes the model architecture,2,PyTorch builds the computation graph dynamically as operations execute (Define-by-Run). This allows standard Python control flow and makes debugging with tools like pdb straightforward.,0.3,DL,PyTorch,computation_graph
"In Keras, which three arguments are required when calling model.compile()?",multiple_choice,optimizer / loss / metrics,layers / activations / dropout,batch_size / epochs / shuffle,input_shape / output_shape / units,1,"model.compile() configures the model for training. optimizer specifies the update rule, loss defines the objective function, and metrics lists the evaluation criteria tracked during training and validation.",0.2,DL,TensorFlow,Keras
Which method must be implemented when subclassing torch.nn.Module in PyTorch?,multiple_choice,run(),call(),forward(),compute(),3,The forward() method defines the computation performed at every call. PyTorch's autograd engine automatically derives the backward pass from the operations recorded in forward().,0.2,DL,PyTorch,nn_module
model.train() in PyTorch enables training-specific behavior in layers such as Dropout and BatchNorm.,true_false,,,,,true,"model.train() sets the model to training mode, enabling stochastic Dropout and batch-level statistics in BatchNorm. This is the counterpart to model.eval() used during inference.",0.1,DL,PyTorch,training_mode
The Keras Functional API supports only sequential (linear) model architectures.,true_false,,,,,false,"The Keras Functional API supports arbitrary DAG (Directed Acyclic Graph) architectures including multi-input, multi-output, and shared-layer models. Only the Sequential API is limited to linear stacks.",0.3,DL,TensorFlow,Keras
torch.no_grad() and model.eval() have identical effects on a PyTorch model.,true_false,,,,,false,model.eval() changes the behavior of layers such as Dropout and BatchNorm. torch.no_grad() disables gradient tracking to save memory and compute. Both are needed together for correct inference.,0.3,DL,PyTorch,inference
What Python class must be subclassed to define a custom model in PyTorch?,text_input,,,,,torch.nn.Module,All custom models and layers in PyTorch are defined by subclassing torch.nn.Module and implementing the __init__() and forward() methods.,0.1,DL,PyTorch,nn_module
What is the name of the TensorFlow Keras API used to build models by stacking layers linearly?,text_input,,,,,Sequential,tf.keras.Sequential allows building models by passing a list of layers or calling model.add(). It is limited to single-input single-output linear stacks of layers.,0.1,DL,TensorFlow,Keras
Which PyTorch class implements the Adam optimization algorithm?,text_input,,,,,torch.optim.Adam,"torch.optim.Adam implements the Adam optimizer, combining momentum and RMSProp-style per-parameter adaptive learning rates for efficient gradient-based optimization.",0.2,DL,PyTorch,optimizer
Which PyTorch loss function is correct for binary classification with sigmoid output?,multiple_choice,torch.nn.CrossEntropyLoss(),torch.nn.BCELoss(),torch.nn.NLLLoss(),torch.nn.MSELoss(),2,"BCELoss computes binary cross-entropy between the target and a predicted probability in [0, 1]. The model output must first pass through a sigmoid. BCEWithLogitsLoss combines sigmoid and BCE in one numerically stable operation.",0.4,DL,PyTorch,loss_functions
What does tf.keras.callbacks.EarlyStopping do during training?,multiple_choice,Stops training when the monitored metric stops improving,Reduces the learning rate after every epoch,Saves the model weights after each epoch,Terminates training after a fixed number of steps,1,EarlyStopping monitors a metric such as val_loss and halts training when it fails to improve for a specified number of epochs (patience). This prevents overfitting and unnecessary computation.,0.3,DL,TensorFlow,callbacks
What interface must be implemented when subclassing torch.utils.data.Dataset?,multiple_choice,__init__ and __call__,__len__ and __getitem__,__iter__ and __next__,__size__ and __fetch__,2,"__len__ returns the total number of samples and __getitem__ returns the sample at a given index. The resulting Dataset object is passed to DataLoader for batching, shuffling, and parallel loading.",0.3,DL,PyTorch,DataLoader
Which tf.data method overlaps data preprocessing and model training to reduce GPU idle time?,multiple_choice,tf.data.Dataset.cache(),tf.data.Dataset.shuffle(),tf.data.Dataset.prefetch(),tf.data.Dataset.repeat(),3,"prefetch(buffer_size) prepares future batches in the background while the GPU processes the current batch, eliminating idle time between steps and improving overall training throughput.",0.5,DL,TensorFlow,data_pipeline
torch.Tensor.detach() creates a new tensor that shares data with the original but is excluded from the autograd graph.,true_false,,,,,true,detach() returns a tensor with the same storage but requires_grad=False and no gradient history. It is used when passing tensors to non-differentiable operations or when logging values without affecting the graph.,0.5,DL,PyTorch,autograd
What is the key difference between torch.nn.CrossEntropyLoss and torch.nn.NLLLoss in PyTorch?,multiple_choice,CrossEntropyLoss applies log_softmax internally while NLLLoss expects log-probabilities as input,CrossEntropyLoss is for binary tasks while NLLLoss is for multi-class tasks,NLLLoss applies softmax internally while CrossEntropyLoss expects raw logits,They are identical in behavior,1,CrossEntropyLoss combines log_softmax and NLLLoss in one step and expects raw logits. NLLLoss requires log-probabilities as input (e.g. via F.log_softmax). Passing the wrong input type causes silent numerical errors.,0.5,DL,PyTorch,loss_functions
What does setting trainable=False on a Keras layer do?,multiple_choice,Removes the layer from the model graph,Prevents the layer weights from being updated during training while still using them in the forward pass,Disables the layer during inference only,Converts the layer weights to constants permanently,2,Setting trainable=False excludes the layer's variables from model.trainable_weights so they receive no gradient updates. This is the standard approach for freezing pretrained layers during transfer learning.,0.5,DL,TensorFlow,transfer_learning
PyTorch's DistributedDataParallel (DDP) is generally preferred over DataParallel (DP) for multi-GPU training.,true_false,,,,,true,"DDP uses one process per GPU and communicates gradients via all-reduce, avoiding the GIL bottleneck and load imbalance of DP's single-process multi-thread model. DDP scales more efficiently across multiple GPUs and nodes.",0.7,DL,PyTorch,distributed_training
What is the difference between torch.nn.functional and torch.nn modules in PyTorch?,multiple_choice,torch.nn.functional contains stateless functions while torch.nn modules hold learnable parameters as attributes,torch.nn.functional only supports CPU while torch.nn supports GPU,torch.nn.functional is deprecated in favor of torch.nn,torch.nn.functional does not support autograd while torch.nn does,1,torch.nn.functional provides pure functions such as F.relu and F.cross_entropy where all arguments must be passed explicitly. torch.nn modules wrap these functions and store parameters as nn.Parameter objects with state.,0.6,DL,PyTorch,nn_module
What does torch.cuda.amp.autocast() enable in PyTorch training?,multiple_choice,Automatic model parallelism across multiple GPUs,Mixed precision execution using float16 and float32 to speed up training and reduce memory,Automatic selection of optimal batch size for available GPU memory,Conversion of a model from float32 to float64,2,autocast() automatically casts eligible operations to float16 while keeping numerically sensitive ones in float32. It is used together with GradScaler which rescales gradients to prevent underflow in float16.,0.7,DL,PyTorch,mixed_precision
What does torch.nn.utils.clip_grad_norm_() implement?,multiple_choice,Weight decay regularization,Gradient clipping by global norm to prevent exploding gradients,Learning rate warmup scheduling,Layer-wise adaptive learning rate scaling,2,clip_grad_norm_() rescales all gradients so their global L2 norm does not exceed a specified max_norm threshold. This stabilizes training in RNNs and Transformers where exploding gradients are a common problem.,0.6,DL,PyTorch,training_stability
"In TensorFlow, what does the embeddings_initializer argument of tf.keras.layers.Embedding control?",multiple_choice,The size of the vocabulary,The dimensionality of the output embedding vectors,How the embedding matrix weights are initialized at the start of training,Whether the embedding weights are frozen during training,3,embeddings_initializer specifies the distribution used to initialize the embedding weight matrix such as uniform or normal. Vocabulary size is controlled by input_dim and vector dimensionality by output_dim.,0.6,DL,TensorFlow,embeddings
What is the role of GradScaler when using mixed precision training in PyTorch?,multiple_choice,It scales the model weights to fit in float16 format,It multiplies the loss by a scale factor before backward to prevent float16 gradient underflow,It automatically selects the precision for each layer independently,It converts the optimizer state to float16 to save memory,2,GradScaler scales the loss upward before backward() so that small gradient values do not underflow to zero in float16. It then unscales gradients before the optimizer step and adjusts the scale factor dynamically.,0.8,DL,PyTorch,mixed_precision
`;
