export const AiPractice_CSV = `
question_text,question_type,option_1,option_2,option_3,option_4,correct_answer,explanation,difficulty,category,subcategory1,subcategory2
Which function returns the length of a Python list?,multiple_choice,len(),size(),count(),length(),1,"len() is Python's built-in function for returning the number of items in a container. size() is used in NumPy, count() counts occurrences of a specific element, and length() does not exist in Python.",0.1,python,basic,list
Which keyword is used for exception handling in Python?,multiple_choice,catch,except,rescue,error,2,"Python uses try/except blocks for exception handling. 'catch' is used in Java and C++, 'rescue' is Ruby's keyword, and 'error' is not an exception-handling keyword in any major language.",0.2,python,error_handling,try_except
True/False: A Python tuple is mutable.,true_false,,,,,false,"Tuples are immutable sequences in Python. Once created, their elements cannot be changed, added, or removed. This immutability makes tuples hashable and usable as dictionary keys, unlike lists.",0.1,python,basic,tuple
Which Big-O notation is the fastest?,multiple_choice,O(1),O(n),O(n log n),O(n^2),1,"O(1) represents constant time complexity — the operation takes the same time regardless of input size. Examples include hash table lookup and array index access. O(n) is linear, O(n log n) is typical for efficient sorting, and O(n^2) is quadratic.",0.2,cs,algorithm,complexity
Which data is suitable for one-hot encoding?,multiple_choice,Ordinal category,Nominal category,Continuous value,Image,2,One-hot encoding creates binary columns for each category and is ideal for nominal (unordered) categories like color or country. Ordinal categories have inherent order and are better served by label encoding. Continuous values and images require different representations.,0.3,ml,preprocessing,encoding
Why is MinMaxScaler weak to outliers?,multiple_choice,Sensitive to outliers,Slow,Ignores categories,Requires labels,1,"MinMaxScaler normalizes features to a fixed range (typically 0-1) using min and max values. A single extreme outlier shifts the min or max dramatically, compressing all other values into a narrow band. StandardScaler or RobustScaler are more resilient alternatives.",0.4,ml,preprocessing,scaling
Which advantage do Transformers have over RNNs?,multiple_choice,Recursion,Parallelization,Lower memory use,Simplicity,2,"Transformers process all positions simultaneously via self-attention, enabling massive parallelization during training. RNNs must process sequences step-by-step, creating a sequential bottleneck. This parallelism allows Transformers to train on much larger datasets efficiently.",0.5,ml,models,transformer
True/False: Embeddings are used for similarity calculation.,true_false,,,,,true,"Embeddings map discrete items (words, sentences, images) into dense continuous vector spaces where geometric distance reflects semantic similarity. Cosine similarity or Euclidean distance between embedding vectors is widely used for search, recommendation, and clustering tasks.",0.2,ml,embeddings,similarity
What issue occurs when top-k=1 in RAG retrieval?,multiple_choice,Missing relevant chunks,Slow inference,Encoding failure,Overfitting,1,"Setting top-k=1 retrieves only the single most similar chunk, which risks missing other relevant passages needed to answer the query completely. Increasing k improves recall at the cost of including some irrelevant context. A typical production setting uses k=3 to k=10.",0.5,ml,rag,retrieval
Name one method to speed up LLM inference.,text_input,,,,,quantization,"Quantization reduces model weights from higher precision (e.g. float32) to lower precision (e.g. int8 or int4), significantly reducing memory footprint and computation time. Other techniques include KV-cache optimization, speculative decoding, and model distillation.",0.4,ml,llm,inference
What Python keyword opens a file safely?,multiple_choice,file(),open(),with open(),new(),3,"The 'with' statement combined with open() creates a context manager that automatically closes the file when the block exits, even if an exception occurs. Using open() alone requires manual close() calls, risking resource leaks.",0.2,python,io,file
True/False: Python lists can hold mixed data types.,true_false,,,,,true,"Python lists are heterogeneous containers that can store objects of any type simultaneously, e.g. [1, 'hello', 3.14, None]. This flexibility comes from Python's dynamic typing — each list element is a reference to an object on the heap.",0.1,python,basic,list
Which operation is fastest for hash maps?,multiple_choice,Insert,Delete,Lookup,All of the above,4,"Hash maps (dictionaries in Python) provide average O(1) time complexity for insert, delete, and lookup operations by computing a hash of the key to directly index into a bucket array. Worst case is O(n) due to hash collisions, but this is rare with good hash functions.",0.3,cs,data_structure,hashmap
What is the purpose of normalization?,multiple_choice,Unify scale,Remove missing values,Encode categories,Remove outliers,1,Normalization rescales features to a common range (e.g. 0-1 for MinMaxScaler or zero mean and unit variance for StandardScaler). This prevents features with larger magnitudes from dominating distance-based algorithms like k-NN and gradient-based optimizers in neural networks.,0.3,ml,preprocessing,norm
Which model is sensitive to the curse of dimensionality?,multiple_choice,Logistic regression,k-NN,Naive Bayes,Decision tree,2,"k-Nearest Neighbors relies on distance metrics to find similar data points. In high-dimensional spaces, distances between all points converge, making the notion of 'nearest' meaningless. This phenomenon is called the curse of dimensionality. Dimensionality reduction (e.g. PCA) is a common mitigation.",0.4,ml,models,knn
What is the goal of K-means?,multiple_choice,Classification,Regression,Clustering,Dimensionality reduction,3,K-means is an unsupervised clustering algorithm that partitions data into k groups by iteratively assigning points to the nearest centroid and updating centroids. It minimizes within-cluster sum of squared distances. It requires specifying k in advance and assumes roughly spherical clusters.,0.3,ml,models,clustering
True/False: PCA is a dimensionality reduction method.,true_false,,,,,true,"Principal Component Analysis (PCA) projects high-dimensional data onto a lower-dimensional subspace defined by the directions of maximum variance (principal components). It preserves as much information as possible while reducing feature count, helping with visualization, noise reduction, and computational efficiency.",0.2,ml,preprocessing,pca
What direction does PCA prioritize?,multiple_choice,Largest variance,Smallest variance,Lowest mean,Random,1,"PCA finds orthogonal axes (principal components) that capture the maximum variance in the data. The first component explains the most variance, the second the next most while being orthogonal to the first, and so on. Directions with minimal variance are discarded as noise.",0.4,ml,preprocessing,pca
What activation helps reduce vanishing gradients?,multiple_choice,Sigmoid,Tanh,ReLU,Linear,3,"ReLU (Rectified Linear Unit) outputs max(0, x), providing a constant gradient of 1 for positive inputs. Sigmoid and tanh saturate at extreme values, producing near-zero gradients that vanish through deep layers during backpropagation. ReLU largely solved this problem, enabling training of much deeper networks.",0.4,ml,dl,activation
Which optimizer adaptively adjusts learning rates?,multiple_choice,SGD,Adam,RMSProp,GD,2,"Adam (Adaptive Moment Estimation) maintains per-parameter learning rates by combining momentum (first moment) and RMSProp (second moment) of gradients. This adaptive behavior helps navigate different curvatures in the loss landscape, typically converging faster than vanilla SGD.",0.5,ml,dl,optimizer
True/False: Dropout prevents overfitting.,true_false,,,,,true,"Dropout randomly zeroes a fraction of neuron activations during each training forward pass (typically 20-50%). This forces the network to learn redundant representations and prevents co-adaptation of neurons, acting as an ensemble of sub-networks. At inference time, all neurons are active but weights are scaled accordingly.",0.3,ml,dl,regularization
What is the purpose of a validation set?,multiple_choice,Train model,Test generalization,Tune hyperparameters,Store errors,3,"The validation set is used during training to evaluate model performance and guide hyperparameter tuning (learning rate, regularization strength, architecture choices) without contaminating the test set. It provides an unbiased estimate of how well the model generalizes to unseen data during development.",0.3,ml,evaluation,validation
Which metric suits classification?,multiple_choice,MSE,Recall,RMSE,MAE,2,"Recall (sensitivity) measures the proportion of actual positive instances correctly identified: TP / (TP + FN). It is essential in domains where missing positive cases is costly, such as medical diagnosis or fraud detection. MSE, RMSE, and MAE are regression metrics that measure continuous prediction error.",0.3,ml,evaluation,classification
What does F1-score balance?,multiple_choice,Precision & recall,Accuracy & loss,MSE & MAE,Recall & error,1,"F1-score is the harmonic mean of precision and recall: F1 = 2 * (P * R) / (P + R). It balances the trade-off between false positives (precision) and false negatives (recall), and is especially useful for imbalanced datasets where accuracy alone can be misleading.",0.4,ml,evaluation,f1
True/False: Overfitting means high training accuracy but low test accuracy.,true_false,,,,,true,"Overfitting occurs when a model memorizes training data patterns (including noise) rather than learning generalizable features. The hallmark is a large gap between training performance (high) and test performance (low). Remedies include regularization, dropout, data augmentation, and early stopping.",0.3,ml,concepts,overfitting
What does tokenization do for LLMs?,multiple_choice,Compress text,Split text into tokens,Translate text,Generate embeddings,2,"Tokenization splits raw text into discrete units (tokens) that the model can process. Common methods include Byte-Pair Encoding (BPE) and SentencePiece, which break words into subword units. For example, 'unhappiness' might become ['un', 'happiness']. Token count determines context window usage and API costs.",0.3,nlp,llm,tokenization
What does cosine similarity measure?,multiple_choice,Angle between vectors,Distance,Probability,Gradient,1,"Cosine similarity computes the cosine of the angle between two vectors, yielding a value from -1 (opposite) to 1 (identical direction). Unlike Euclidean distance, it is magnitude-invariant, making it ideal for comparing text embeddings where direction (semantic meaning) matters more than length.",0.3,ml,similarity,cosine
Which algorithm is used in vector search?,multiple_choice,KD-tree,HNSW,DFS,BFS,2,"Hierarchical Navigable Small World (HNSW) is an approximate nearest neighbor (ANN) algorithm that builds a multi-layer graph where each node connects to nearby neighbors. It achieves sub-linear query time with high recall, making it the default index type in vector databases like Pinecone, Milvus, and pgvector.",0.5,ml,vector_search,ann
True/False: Embeddings with larger dimensions always perform better.,true_false,,,,,false,"Higher-dimensional embeddings can capture more nuance but also introduce noise, increase computational cost, and require more training data to fill the space meaningfully. OpenAI's text-embedding-3-small (1536-dim) often performs comparably to larger models for many tasks. The optimal dimension depends on the dataset and use case.",0.4,ml,embeddings,dims
What improves RAG retrieval quality?,multiple_choice,Larger chunk size,More stopwords,Lower embedding dimension,Single document,1,"Larger chunks provide more context per retrieval hit, reducing the chance of returning a fragment that lacks the full answer. However, too-large chunks dilute relevance and waste context window space. Typical production systems use 256-1024 tokens per chunk with overlap for best results.",0.4,ml,rag,chunking
What is the role of a vector DB?,multiple_choice,Store numbers only,Store vectors & search,Compile code,Translate text,2,"Vector databases (e.g. Pinecone, Weaviate, Qdrant) store high-dimensional embedding vectors and provide efficient approximate nearest neighbor (ANN) search. They support CRUD operations on vectors plus metadata filtering, enabling semantic search, recommendation, and RAG pipelines at scale.",0.4,system,vector_db,ann
What is the purpose of learning rate?,multiple_choice,Control update size,Control dataset size,Control epochs,Control memory,1,The learning rate controls the step size of each weight update during gradient descent. Too high causes the optimizer to overshoot minima and diverge; too low leads to extremely slow convergence or getting stuck in local minima. Learning rate scheduling and warmup strategies help balance speed and stability.,0.3,ml,dl,training
True/False: Too high learning rate causes divergence.,true_false,,,,,true,"When the learning rate is too large, gradient descent updates overshoot the loss minimum, causing the loss to increase instead of decrease. In extreme cases, weights grow unbounded (diverge). Techniques like learning rate warmup, cosine annealing, and gradient clipping help prevent this.",0.3,ml,dl,training
What file format is typically used for training data?,multiple_choice,jpg,csv,pdf,exe,2,"CSV (Comma-Separated Values) is the most common format for structured tabular training data due to its simplicity, human readability, and universal tool support. For larger datasets, Parquet (columnar) or JSONL (semi-structured) are preferred for efficiency. Image data uses jpg/png but organized via CSV manifests.",0.1,data,storage,file
What database is suitable for logs?,multiple_choice,SQL,Redis,Time-series DB,Graph DB,3,"Time-series databases (e.g. InfluxDB, TimescaleDB, Amazon Timestream) are optimized for timestamped data with high write throughput, time-range queries, and automatic data retention policies. Log data is inherently time-ordered and append-heavy, making time-series DBs a natural fit over general-purpose SQL or key-value stores.",0.4,system,database,time_series
What cloud service hosts a container?,multiple_choice,S3,Lambda,ECS,RDS,3,"Amazon ECS (Elastic Container Service) orchestrates Docker containers on managed EC2 or Fargate infrastructure. S3 is object storage, Lambda is serverless functions (not full containers), and RDS is managed relational databases. EKS (Kubernetes) is another AWS container option for more complex orchestration needs.",0.4,cloud,aws,ecs
True/False: S3 is an object storage service.,true_false,,,,,true,"Amazon S3 (Simple Storage Service) stores data as objects in buckets with virtually unlimited scalability. Each object consists of data, metadata, and a unique key. Unlike block storage (EBS) or file storage (EFS), S3 is optimized for durability (99.999999999%) and HTTP-based access patterns.",0.2,cloud,aws,s3
What is REST API's principle?,multiple_choice,Stateful,Stateless,Real-time only,Encrypted only,2,"REST (Representational State Transfer) requires each request to contain all information needed for the server to process it — no session state is stored server-side between requests. This statelessness enables horizontal scaling, caching, and load balancing. Authentication tokens (e.g. JWT) are passed with each request.",0.3,api,rest,design
What status code means success?,multiple_choice,200,301,404,500,1,"HTTP 200 OK indicates the request succeeded. 301 is a permanent redirect, 404 means the resource was not found, and 500 is an internal server error. The 2xx range covers success codes: 200 (OK), 201 (Created), 204 (No Content).",0.1,api,http,status
What status code indicates client error?,multiple_choice,100,200,400,500,3,"HTTP 400 Bad Request is a generic client error indicating the server cannot process the request due to malformed syntax. The 4xx range covers all client errors: 401 (Unauthorized), 403 (Forbidden), 404 (Not Found), 422 (Unprocessable Entity), 429 (Too Many Requests).",0.1,api,http,status
True/False: Webhooks push data to the client.,true_false,,,,,true,"Webhooks are HTTP callbacks that push event notifications from a server to a client-specified URL via POST requests when an event occurs. Unlike polling (client repeatedly asking for updates), webhooks are event-driven and real-time, reducing unnecessary network traffic and latency.",0.3,api,webhook,design
What does JWT store?,multiple_choice,Session state,User claims,Images,HTML,2,"JWT (JSON Web Token) encodes a JSON payload of claims (user ID, roles, expiration) signed with a secret or key pair. The server can verify the token's integrity without database lookups, enabling stateless authentication. JWTs consist of three base64url-encoded parts: header, payload, and signature.",0.3,security,jwt,auth
What encryption is used in HTTPS?,multiple_choice,AES only,RSA only,TLS,TCP,3,"HTTPS uses TLS (Transport Layer Security) to encrypt communication between client and server. TLS negotiates a secure channel using asymmetric encryption (RSA or ECDHE) for key exchange, then switches to symmetric encryption (AES) for data transfer. TCP is a transport protocol, not an encryption standard.",0.4,security,network,tls
True/False: SQL injection exploits unsafe queries.,true_false,,,,,true,"SQL injection occurs when user input is concatenated directly into SQL queries without sanitization, allowing attackers to execute arbitrary SQL. For example, input like ' OR 1=1 -- can bypass authentication. Parameterized queries (prepared statements) and ORM frameworks are the primary defenses.",0.3,security,sql,attack
What is the purpose of indexing in SQL?,multiple_choice,Store duplicates,Speed up search,Encrypt data,Compress tables,2,"Database indexes create sorted data structures (typically B-trees) that enable the query engine to locate rows without scanning the entire table. A well-chosen index can reduce query time from O(n) to O(log n). However, indexes add write overhead and storage cost, so they should be applied selectively.",0.4,db,sql,index
What join returns only matching rows?,multiple_choice,INNER,LEFT,RIGHT,FULL,1,"INNER JOIN returns only rows where the join condition is satisfied in both tables. LEFT JOIN includes all rows from the left table (NULLs for non-matching right), RIGHT JOIN does the opposite, and FULL OUTER JOIN includes all rows from both tables with NULLs where no match exists.",0.3,db,sql,join
What is ACID in databases?,multiple_choice,API rules,Transaction guarantees,Caching rules,Search protocol,2,"ACID stands for Atomicity (all-or-nothing), Consistency (valid state transitions), Isolation (concurrent transactions don't interfere), and Durability (committed data survives crashes). These properties ensure reliable transaction processing in relational databases like PostgreSQL and MySQL.",0.5,db,concept,acid
What does NoSQL emphasize?,multiple_choice,Joins,Fixed schema,Scalability,ACID,3,"NoSQL databases (MongoDB, DynamoDB, Cassandra) prioritize horizontal scalability and flexible schemas over strict ACID compliance. They use eventual consistency models and partition data across nodes for high throughput. This makes them well-suited for large-scale distributed applications where schema evolution is frequent.",0.4,db,nosql,concept
True/False: Redis is an in-memory database.,true_false,,,,,true,"Redis stores all data in RAM for sub-millisecond read/write latency. It supports data structures like strings, hashes, lists, sets, and sorted sets. Persistence options (RDB snapshots, AOF logging) allow durability, but primary access is always from memory. Common uses include caching, session storage, and real-time leaderboards.",0.3,db,redis,cache
What is an API rate limit for?,multiple_choice,Improve speed,Prevent abuse,Remove headers,Compress responses,2,"Rate limiting restricts the number of API requests a client can make within a time window (e.g. 100 requests/minute). It protects against DDoS attacks, prevents resource exhaustion, ensures fair usage among clients, and controls infrastructure costs. HTTP 429 (Too Many Requests) is returned when limits are exceeded.",0.3,api,security,limit
What is overfitting?,multiple_choice,High bias,Low variance,Memorizing training data,Low training accuracy,3,"Overfitting occurs when a model captures noise and spurious patterns in training data instead of the underlying distribution. The model achieves high training accuracy but fails to generalize to unseen data. It is characterized by low bias and high variance. Common countermeasures include regularization, dropout, and early stopping.",0.4,ml,concept,overfitting
What technique reduces overfitting?,multiple_choice,Increase model size,Dropout,Remove regularization,Increase noise,2,"Dropout randomly deactivates neurons during training (typically with probability 0.2-0.5), forcing the network to learn robust features that are not dependent on specific neuron co-activations. At inference, all neurons are active. Other overfitting countermeasures include L1/L2 regularization, data augmentation, and early stopping.",0.3,ml,dl,regularization
What is cross-validation used for?,multiple_choice,Speed up training,Model evaluation,Data generation,Loss tuning,2,"Cross-validation partitions the dataset into k folds, trains on k-1 folds, and validates on the remaining fold, rotating through all folds. This provides a more reliable performance estimate than a single train/test split, especially with limited data. It also helps detect overfitting and guides hyperparameter selection.",0.4,ml,evaluation,cv
True/False: k-fold CV trains k models.,true_false,,,,,true,"In k-fold cross-validation, the dataset is split into k equal folds. The model is trained k times, each time using a different fold as the validation set and the remaining k-1 folds for training. The final metric is the average across all k runs, providing a robust performance estimate.",0.4,ml,evaluation,cv
What is the purpose of softmax?,multiple_choice,Normalize probabilities,Reduce dimension,Encode labels,Sort values,1,"Softmax converts a vector of raw logits into a probability distribution where all values are in (0,1) and sum to 1. It exponentiates each element and divides by the sum of all exponentials. It is used as the final activation in multi-class classification to produce class probabilities.",0.4,ml,dl,activation
What gradient issue does deep networks face?,multiple_choice,Exploding/vanishing,Broken layers,Slow I/O,Missing labels,1,"During backpropagation through many layers, gradients are multiplied repeatedly. With activation functions like sigmoid, gradients shrink exponentially (vanishing), preventing early layers from learning. Conversely, large weights can cause gradients to grow exponentially (exploding). Solutions include ReLU activation, batch normalization, residual connections, and gradient clipping.",0.5,ml,dl,training
What helps with exploding gradients?,multiple_choice,Dropout,Gradient clipping,Bigger model,More epochs,2,"Gradient clipping rescales gradients when their norm exceeds a threshold, preventing extremely large weight updates that destabilize training. It is essential for training RNNs and Transformers. PyTorch provides torch.nn.utils.clip_grad_norm_() for this purpose. It does not eliminate the root cause but keeps training stable.",0.5,ml,dl,training
True/False: BatchNorm stabilizes training.,true_false,,,,,true,"Batch Normalization normalizes each layer's inputs to zero mean and unit variance within each mini-batch, then applies learned scale and shift parameters. This reduces internal covariate shift, allows higher learning rates, acts as mild regularization, and significantly accelerates convergence in deep networks.",0.4,ml,dl,batchnorm
What is fine-tuning?,multiple_choice,Train from scratch,Update all weights on small data,Remove layers,Freeze optimizer,2,Fine-tuning takes a pretrained model (trained on a large general dataset) and continues training on a smaller domain-specific dataset. All or some weights are updated with a low learning rate to adapt the model's knowledge to the target task while retaining useful learned representations. It is the basis of transfer learning.,0.4,ml,llm,fine_tuning
What is prompt engineering?,multiple_choice,Change model weights,Design effective prompts,Store data,Debug code,2,"Prompt engineering is the practice of designing input text (prompts) to guide LLM behavior without modifying model weights. Techniques include zero-shot, few-shot examples, chain-of-thought reasoning, role assignment, and output format instructions. Effective prompts can dramatically improve accuracy, consistency, and task adherence.",0.3,nlp,llm,prompt
What does temperature control?,multiple_choice,Model size,Sampling randomness,Token length,Embedding scale,2,"Temperature scales the logits before softmax during token sampling. A value of 0 makes the model deterministic (always picking the highest-probability token), while higher values (e.g. 1.0-2.0) increase randomness and creativity. Lower temperatures (0.1-0.3) are preferred for factual or structured outputs.",0.3,nlp,llm,inference
True/False: Top-p sampling filters tokens by cumulative probability.,true_false,,,,,true,"Top-p (nucleus) sampling selects from the smallest set of tokens whose cumulative probability exceeds p (e.g. 0.9). Unlike top-k which always considers a fixed number of tokens, top-p dynamically adjusts the candidate pool size based on the probability distribution, producing more natural text.",0.4,nlp,llm,decoding
What speeds up vector search?,multiple_choice,Larger vectors,HNSW,Sorting by hand,More partitions,2,HNSW (Hierarchical Navigable Small World) constructs a multi-layer proximity graph enabling O(log n) approximate nearest neighbor queries. It offers an excellent speed-recall trade-off and is the default index in most vector databases. Other ANN algorithms include IVF (Inverted File Index) and ScaNN.,0.5,ml,vector_search,ann
What improves RAG answer accuracy?,multiple_choice,More retrieval chunks,Fewer embeddings,Shorter prompts,Single document,1,"Retrieving more chunks (higher top-k) increases the likelihood of including the passage that contains the answer, improving recall. However, too many chunks can introduce noise and exceed the LLM's context window. Strategies like re-ranking retrieved chunks and hybrid search (combining keyword + semantic) further improve accuracy.",0.5,ml,rag,retrieval
What is the purpose of cosine similarity?,multiple_choice,Angle comparison,Length comparison,Sorting tokens,Compression,1,"Cosine similarity measures the cosine of the angle between two vectors, focusing on orientation rather than magnitude. A value of 1 means identical direction, 0 means orthogonal, and -1 means opposite. It is the standard metric for comparing text embeddings because document length should not affect semantic similarity.",0.3,ml,similarity,cosine
What is the goal of regularization?,multiple_choice,Increase accuracy,Prevent overfitting,Train faster,Add noise,2,"Regularization adds a penalty term to the loss function that discourages overly complex models. L1 regularization (lasso) encourages sparse weights, L2 regularization (ridge) penalizes large weights. Both constrain the model's capacity, reducing variance and improving generalization to unseen data.",0.3,ml,concept,regularization
True/False: L2 regularization penalizes large weights.,true_false,,,,,true,"L2 regularization adds the sum of squared weights (multiplied by a lambda coefficient) to the loss function: Loss + lambda * sum(w^2). This penalty discourages any single weight from growing too large, effectively smoothing the model's decision boundary and reducing overfitting.",0.3,ml,dl,regularization
What is a hyperparameter?,multiple_choice,Trainable weight,Model setting,Dataset item,Loss value,2,"Hyperparameters are configuration values set before training begins (learning rate, batch size, number of layers, dropout rate). Unlike model parameters (weights and biases) which are learned during training, hyperparameters are tuned via grid search, random search, or Bayesian optimization using validation performance.",0.3,ml,concept,hyperparameter
What is the purpose of an epoch?,multiple_choice,One full pass of dataset,One batch,One prediction,One loss update,1,One epoch means the model has seen every training example exactly once. Training typically runs for multiple epochs (e.g. 10-100) so the model can refine weights iteratively. Too many epochs risk overfitting; early stopping monitors validation loss to determine the optimal number.,0.2,ml,dl,training
Which metric suits regression?,multiple_choice,Accuracy,F1,MSE,Recall,3,"Mean Squared Error (MSE) averages the squared differences between predicted and actual values. It penalizes large errors more heavily than small ones due to squaring. RMSE (root of MSE) is in the same unit as the target. Accuracy, F1, and Recall are classification metrics that require discrete labels.",0.3,ml,evaluation,regression
True/False: Larger models always outperform smaller ones.,true_false,,,,,false,"Larger models have more capacity but also higher risk of overfitting, especially with limited training data. They require more compute, memory, and data to train effectively. In practice, a well-regularized smaller model often outperforms an overparameterized large model. Model selection should balance capacity with available resources.",0.3,ml,concept,bias_variance
What is gradient descent used for?,multiple_choice,Find minima,Sort data,Encode labels,Split data,1,"Gradient descent is an iterative optimization algorithm that minimizes a loss function by updating parameters in the direction of the negative gradient. Each step moves weights proportionally to the learning rate. Variants include SGD (stochastic), mini-batch SGD, and adaptive methods like Adam.",0.2,ml,optimization,gd
What is backpropagation?,multiple_choice,Forward pass,Error propagation,Data cleaning,Preprocessing,2,"Backpropagation efficiently computes gradients of the loss with respect to each weight by applying the chain rule in reverse through the network. Starting from the output layer, it propagates error signals backward, enabling gradient descent to update all weights in a single backward pass.",0.4,ml,dl,training
What improves training stability?,multiple_choice,Large LR,Small batch size,Learning rate scheduling,Random layers,3,"Learning rate scheduling adjusts the learning rate during training — starting high for fast initial progress, then decaying for fine-grained convergence. Popular strategies include step decay, cosine annealing, and warmup-then-decay. This prevents oscillation around minima and helps escape sharp local minima.",0.4,ml,dl,training
True/False: Self-attention allows global dependency modeling.,true_false,,,,,true,"In self-attention, each token computes attention weights with every other token in the sequence, enabling direct access to information at any position regardless of distance. This contrasts with RNNs where distant information must propagate through many sequential steps, often degrading along the way.",0.4,ml,models,transformer
What is a common embedding distance metric?,multiple_choice,Manhattan,Cosine,Hamming,Jaccard,2,"Cosine similarity is the most widely used metric for comparing dense embedding vectors because it measures directional similarity independent of vector magnitude. Manhattan distance (L1) sums absolute differences, Hamming counts bit differences in binary codes, and Jaccard compares set overlap.",0.3,ml,embeddings,distance
What is the purpose of a loss function?,multiple_choice,Measure error,Sort data,Encode values,Generate text,1,"A loss function quantifies how far the model's predictions are from the true values, providing a scalar signal that guides optimization. Common loss functions include cross-entropy (classification), MSE (regression), and contrastive loss (similarity learning). The choice of loss function directly shapes what the model learns.",0.2,ml,concept,loss
What does ROC-AUC measure?,multiple_choice,Class separation,Loss minimum,Model speed,Embedding quality,1,"ROC-AUC (Receiver Operating Characteristic - Area Under Curve) measures a classifier's ability to distinguish between classes across all decision thresholds. AUC of 1.0 means perfect separation, 0.5 means random guessing. It is threshold-independent, making it useful for comparing models before choosing an operating point.",0.5,ml,evaluation,roc
What helps reduce model size?,multiple_choice,Pruning,More layers,Bigger optimizer,Duplicate weights,1,"Pruning removes weights or neurons with minimal impact on model performance, reducing parameter count and inference cost. Structured pruning removes entire channels or attention heads; unstructured pruning zeros individual weights. Combined with quantization and distillation, pruning can shrink models by 10x or more.",0.4,ml,dl,pruning
True/False: Quantization reduces memory usage.,true_false,,,,,true,"Quantization converts model weights from high-precision formats (float32) to lower-precision (float16, int8, or int4), dramatically reducing memory footprint and accelerating inference on compatible hardware. For example, int8 quantization reduces model size by approximately 4x compared to float32 with minimal accuracy loss.",0.3,ml,llm,quantization
What improves throughput in LLM inference?,multiple_choice,Batching,Longer prompts,High temperature,Full precision,1,"Batching groups multiple inference requests together to maximize GPU utilization. GPUs excel at parallel computation, so processing 8 requests simultaneously is far more efficient than processing them one-by-one. Continuous batching (as in vLLM) further improves throughput by dynamically filling batch slots as requests complete.",0.4,ml,llm,inference
What is a typical vector dimension?,multiple_choice,8,32,768,10000,3,"768 dimensions is common among popular embedding models (BERT-base, sentence-transformers). OpenAI's text-embedding-3-small uses 1536 dimensions. The dimension balances expressiveness (more dims capture more nuance) against computational cost (search and storage scale with dimension count).",0.3,ml,embeddings,dims
What increases embedding quality?,multiple_choice,Clean text,Random noise,Short sentences only,Stopwords only,1,"Clean, well-structured input text produces higher-quality embeddings because the model can focus on semantic content rather than noise. Preprocessing steps include removing HTML tags, fixing encoding issues, normalizing whitespace, and ensuring text is contextually complete. Garbage in, garbage out applies strongly to embeddings.",0.3,ml,embeddings,data
True/False: Larger chunk size in RAG increases context.,true_false,,,,,true,"Larger chunks contain more surrounding context, making it easier for the LLM to understand the full meaning. However, excessively large chunks dilute the relevant information with irrelevant text and consume more of the limited context window. Typical chunk sizes range from 256 to 1024 tokens with 10-20% overlap between consecutive chunks.",0.3,ml,rag,chunking
What is a GPU mainly used for?,multiple_choice,Matrix operations,Network routing,Disk IO,Caching,1,"GPUs contain thousands of cores optimized for parallel matrix operations (SIMD architecture). Deep learning relies heavily on matrix multiplications (weight × input), making GPUs 10-100x faster than CPUs for training and inference. NVIDIA's CUDA platform is the dominant GPU computing framework for ML workloads.",0.2,system,hardware,gpu
What tensor format does PyTorch use?,multiple_choice,pTensor,Tensor,Matrix,Array,2,"PyTorch's fundamental data structure is torch.Tensor — a multi-dimensional array supporting GPU acceleration and automatic differentiation. Tensors are similar to NumPy ndarrays but with additional capabilities: GPU computation via .cuda(), gradient tracking via requires_grad, and integration with PyTorch's autograd engine.",0.2,python,pytorch,tensor
What is the purpose of DataLoader?,multiple_choice,Load data in batches,Train model,Log metrics,Store weights,1,"PyTorch's DataLoader wraps a Dataset and provides batching, shuffling, parallel data loading (via num_workers), and optional custom collation. It yields batches of tensors during training, decoupling data preparation from model computation and enabling efficient GPU utilization by prefetching data on CPU workers.",0.3,python,pytorch,dataloader
True/False: Autograd computes gradients automatically.,true_false,,,,,true,"PyTorch's autograd engine automatically records all operations on tensors with requires_grad=True, building a dynamic computational graph. Calling loss.backward() traverses this graph in reverse to compute gradients for every parameter, enabling gradient-based optimization without manually deriving derivatives.",0.3,python,pytorch,autograd
What is an embedding layer used for?,multiple_choice,Convert tokens to vectors,Decode images,Sort text,Compress folders,1,"An embedding layer (e.g. nn.Embedding in PyTorch) is a learnable lookup table that maps discrete integer indices (token IDs, item IDs) to dense continuous vectors. During training, these vectors are updated to capture semantic relationships. Pretrained embeddings like Word2Vec or GloVe can initialize the layer.",0.3,nlp,embedding,layer
`;
