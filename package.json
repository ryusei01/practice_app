{
  "name": "quiz-marketplace-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install --legacy-peer-deps && npx expo export --platform web"
  },
  "dependencies": {
    "react-native-fs": "^2.20.0"
  },
  "devDependencies": {
    "babel-plugin-module-resolver": "^5.0.2"
  }
}
